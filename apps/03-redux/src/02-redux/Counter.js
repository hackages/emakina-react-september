import React from 'react';

export const Counter = () => {
  // TODO: connect to redux store to get the counter value.
  // TIP: you'll need "useSelector" from react-redux for that
  const count = 0;
  return <p>Counter: {count}</p>;
};
