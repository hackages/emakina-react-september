import React from "react";

export const App = () => {
  // TODO:
  // - initialize the state with a value that will contain the input content
  // - implement the function that will handle the input change

  return (
    <div className="wrapper flex fxcolumn">
      {/* TODO: bind the input value from the state to the input */}
      {/* bind the handle function you created to the input */}
      <input type="text" />
      {/* TODO: replace the hard-coded name with the one in the state */}
      <p>My name is James</p>
    </div>
  );
};
